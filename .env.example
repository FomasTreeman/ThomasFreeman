# Vercel Blob Storage
# Add this to your .env.local file (for local development) 
# or .env file (both work, but .env.local is preferred for local dev)
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Get your token from: https://vercel.com/dashboard/stores
# Make sure to restart your dev server after adding the token
